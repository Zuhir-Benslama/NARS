# Optional: set this to point at an API on a different origin.
# Leave empty (default) — the Vite dev proxy handles /api/* locally,
# and in production the backend is on the same origin.
VITE_API_BASE=
