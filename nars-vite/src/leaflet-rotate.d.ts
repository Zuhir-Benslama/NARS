declare module 'leaflet-rotate' {
    // leaflet-rotate patches L.Map with rotate option and setBearing method.
    // No named exports needed — importing the module is sufficient.
    const plugin: unknown
    export default plugin
}
