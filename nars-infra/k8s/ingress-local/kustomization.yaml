---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Dev-only local ingresses. This is a standalone kustomization (not part of
# the base) so the dev overlay can add it without polluting production
# builds. The base (../) never references this directory.
resources:
  - ingress-local.yaml
