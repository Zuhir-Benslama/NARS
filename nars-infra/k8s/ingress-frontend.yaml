---
# ─── Frontend Ingress ──────────────────────────────────────────────────
# Serves nars.dz (SPA + API) with TLS but NO mTLS (browser-facing).
# /api/* routes to the API service; /(.*) routes to the SPA.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nars-frontend
  namespace: nars
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - nars.dz
      secretName: nars-tls
  rules:
    - host: nars.dz
      http:
        paths:
          - path: /health
            pathType: Exact
            backend:
              service:
                name: nars-api
                port:
                  number: 80
          - path: /api(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: nars-api
                port:
                  number: 80
          - path: /(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: nars-frontend
                port:
                  number: 80
---
# ─── Frontend Ingress (local dev) ──────────────────────────────────────
# Serves localhost without TLS for local development access.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nars-frontend-local
  namespace: nars
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: localhost
      http:
        paths:
          - path: /(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: nars-frontend
                port:
                  number: 80
