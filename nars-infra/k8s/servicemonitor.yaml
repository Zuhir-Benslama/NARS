---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: otel-collector
  namespace: observability
  labels:
    # Kept for documentation: required only if serviceMonitorSelector is configured
    # with this label. Current selector config picks up ALL ServiceMonitors.
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: opentelemetry-collector
      app.kubernetes.io/instance: otel-collector
      app.kubernetes.io/component: metrics
  namespaceSelector:
    matchNames:
      - observability
  endpoints:
    - port: prometheus-exporter
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
