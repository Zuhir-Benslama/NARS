# nars/nars-infra — Code Quality Fixes

## Done

### C2 (Security): AllowedHosts restricted
- `k8s/configmap.yaml`: Changed `AllowedHosts: "*"` → `AllowedHosts: "nars.dz;api.nars.dz;localhost"`
- `k8s/app-deployment.yaml`: Added `httpHeaders: [{name: Host, value: nars.dz}]` to all 3 probes so probes use a matching Host header

### m1 (Code Smell): Deduplicated nginx proxy config
- Created `docker/proxy-common-snippet.conf` with shared `proxy_set_header`/`proxy_http_version` directives
- `docker/nginx.nars-vite.conf`: Both `/api/` and `/login` blocks now `include` the snippet instead of duplicating
- `docker/Dockerfile.nars-vite`: Copies snippet to `/etc/nginx/snippets/proxy-common.conf`

### m2+m3 (Config): HEALTHCHECK cleanup
- `docker/Dockerfile.nars-api`: Removed redundant `|| exit 1`; added `--connect-timeout 3 --max-time 5`

## Already clean (verified, no action needed)

- **C1**: `k8s/certs/ca.key` is already excluded by `.gitignore` and was never committed — file exists on disk for local dev only
- **m4**: `scripts/__pycache__/` is already excluded by `.gitignore` and was never committed — auto-generated by running the Python script
- **M1**: ServiceAccounts don't need RBAC rules — none of the three pods interact with the Kubernetes API
- **M2**: OTel TLS disabled by default is intentional for dev; production config is well-documented in comments
- **M3**: `latest` tags are documented as dev-only with explicit CI/CD override instructions
- **M4**: `hostPath` volume has a clear warning and is documented as dev-only
