# mdl (markdownlint Ruby gem) style for docs markdown.
# Loaded by `make infra-lint-markdown` via `mdl -s nars-infra/.markdownlint.rb`.
#
# Keep the full default rule set; the docs' prose, tables, and mermaid blocks
# intentionally exceed 80 columns.
exclude_rule 'MD013'